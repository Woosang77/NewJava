**CH08_인터페이스**

- 인터페이스 : 객체의 사용방법을 정의한 타입.
- 선언 : [public] interface 인터페이스명 {...}
    구성멤버 : 상수, 메소드(추상, 디폴트, 정적)
        --> 클래스는 필드, 생성자, 메소드를 멤버로 가지지만,
            인터페이스는 자체적으로 객체를 생성할 수 없어 큰 틀만 가지고 있다.

        interface 인터페이스명 {
            //상수
            타입 상수명 = 값;       (타입 앞에 public static final이 숨어있다 생각하면 됨)
            - 수정불가

            // 추상메소드
            타입 메소드명(매개변수);  (타입 앞에 abstract 숨어있다 생각하면됨)

            //디폴트 메소드 : 인터페이스의 확장을 위해 기능이 추가됨
            default 타입 메소드명(매개변수) {...}
                - 인터페이스의 기능을 추가할 때마다 구현하는 모든 객체를 수정하는 수고를 덜기 위해
                  인터페이스에 추가하면 바로 사용할 수 있도록 개발
                - 중괄호{}를 통해 실행내용 작성

            //정적 메소드
            static 타입 메소드명(매개변수) {...}
        }